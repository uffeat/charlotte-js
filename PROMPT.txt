Please give me a JS version of the Python script below. 
Please keep the snake case for method names.
Please add the JSDoc style comments.
Please also let me know if the code makes sense or if you detect any mistakes.


Please give me a JS function `parseHash` function that takes `window.location.hash` string as a `hash` arg.
The function should return an object with the following keys:
- `key`: A string correponding to the part of `window.location.hash` between `#` and the first '/'.
  If no '/' is found in `window.location.hash`, `key` should hold the string after `#`.
- params: 



Comments

Possibly encoded
