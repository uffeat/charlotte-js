Please give me a JS version of the Python script below. 
Please keep the snake case for method names.
Please add the JSDoc style comments.
Please also let me know if the code makes sense or if you detect any mistakes.


Please give me the reverse of the functions below, i.e., a `genSearchObj` function that takes
an encoded url search params string as arg and returns a correponding decoded "search object".

/** Returns encoded search params string form search object. */
function genSearchParams(searchObj) {
  const urlParams = new URLSearchParams();
  for (const [key, value] of Object.entries(searchObj)) {
    urlParams.set(key, value);
  }
  return "?" + urlParams.toString();
}